# ===================================
#  Form Field Definitions
# ===================================

fields:

    name:
        label: istheweb.iscorporate::lang.fields.name

    is_active:
        label: istheweb.iscorporate::lang.fields.is_active
        type: switch
        default: true
